{
  "name": "thru",
  "version": "0.1.0",
  "description": "Thru game",
  "main": "src/index.js",
  "author": "gugiserman@gmail.com",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html --open --out-dir build --port 3000",
    "build": "parcel build src/index.html --out-dir dist"
  },
  "dependencies": {
    "domready": "^1.0.8",
    "normalize.css": "^8.0.1"
  }
}
